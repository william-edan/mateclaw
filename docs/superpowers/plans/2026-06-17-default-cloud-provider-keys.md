# DashScope / DeepSeek 默认版云端模型 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增三个内置「默认版」云端 provider（`dashscope-default` / `dashscope-compat-default` / `deepseek-default`），预置配置文件里的平台 key、开箱即用、key 隐藏只读不可删除；原版 `dashscope` / `dashscope-compat` / `deepseek` 改为「自带 key」（空 key、默认不启用）。

**Architecture:** 后端引入「受管默认 provider id 集合」作为单一来源，驱动平台 key 注入、`managedKey` DTO 标记、`updateProviderConfig` 只读守卫。平台 key 由 `application.yml` 的 `mateclaw.llm.default-provider-keys.{dashscope,deepseek}` 注入（尊重 env 覆盖），通过一个幂等的 `applyManagedDefaultProviderKeys()` 方法在启动时（web 模式）与 `/setup/init` 后（桌面延迟 seed 模式）对所有工作区回填。数据交付分两条路径：全新库改 4 个种子文件，老库走 Flyway `V145` 迁移按工作区补齐。前端按 `managedKey` 锁定 key 输入与删除入口。

**Tech Stack:** Java 21 + Spring Boot + MyBatis-Plus（后端，构建必须用 JDK 21）；Flyway（H2 + MySQL 双方言迁移）；Vue 3 + TypeScript + vue-i18n（前端）。

---

## 背景速查（实现前必读）

- 受管 provider id（本计划核心常量，多处复用）：
  `dashscope-default`、`dashscope-compat-default`、`deepseek-default`
- 平台 key 配置（已存在）：`mateclaw-server/src/main/resources/application.yml:206-208`
  - `default-provider-keys.dashscope`、`default-provider-keys.deepseek`
  - 映射：两个 dashscope 默认版都用 `dashscope` 字段；deepseek 默认版用 `deepseek` 字段。
- `mate_model_provider` 运行时 schema（post-V133）：PK 是 `id` BIGINT（H2 `GENERATED BY DEFAULT AS IDENTITY`），并有 `UNIQUE(workspace_id, provider_id)`、`enabled BOOLEAN DEFAULT FALSE`、`workspace_id BIGINT DEFAULT 1`、`fallback_priority INT DEFAULT 0`。`schema.sql` 是 pre-V133 旧定义，不是运行时真相。
- `mate_model_config`：`provider`(VARCHAR FK)、`workspace_id`、`is_default`、`builtin`、`model_type`，post-V133 还有 `max_input_tokens, enable_search, search_strategy, modalities, request_timeout_seconds`。
- 种子仅在全新库首次启动跑（`DatabaseBootstrapRunner`，Flyway 之后）；老库改动必须用新 Flyway 迁移。最新迁移版本 **V144** → 新增 **V145**（h2 + mysql 各一份）。
- 桌面模式种子延迟到 `POST /api/v1/setup/init`（`SetupController.init → bootstrapRunner.initWithLocale`），`SetupController` 已注入 `ModelProviderService`。

## 文件结构（创建/修改清单）

后端：
- 修改 `mateclaw-server/src/main/java/vip/mate/llm/service/ModelProviderService.java`
  - 新增受管集合常量 + `isManagedDefaultProvider` + `managedDefaultKeyFor`
  - 改 `defaultProviderKey()` 映射
  - `toProviderInfo()` 设置 `managedKey`
  - `updateProviderConfig()` 只读守卫
  - 新增 `applyManagedDefaultProviderKeys()`
- 修改 `mateclaw-server/src/main/java/vip/mate/llm/model/ProviderInfoDTO.java`（加 `managedKey` 字段）
- 新建 `mateclaw-server/src/main/java/vip/mate/llm/config/DefaultProviderKeyBootstrap.java`（`ApplicationRunner`，启动后回填）
- 修改 `mateclaw-server/src/main/java/vip/mate/system/controller/SetupController.java`（`/setup/init` 成功后回填）

数据：
- 修改 4 个种子文件：`db/data-zh.sql`、`db/data-en.sql`、`db/data-mysql-zh.sql`、`db/data-mysql-en.sql`
- 新建 `db/migration/h2/V145__seed_default_cloud_provider_keys.sql`
- 新建 `db/migration/mysql/V145__seed_default_cloud_provider_keys.sql`

前端：
- 修改 `mateclaw-ui/src/types/index.ts`（`ProviderInfo.managedKey`）
- 修改 `mateclaw-ui/src/views/Settings/Models/ProviderCard.vue`（锁定 key 展示）
- 修改 `mateclaw-ui/src/views/Settings/Models/modals/ProviderConfigModal.vue`（禁用 key/baseUrl 输入）
- 修改 `mateclaw-ui/src/i18n/locales/zh-CN.ts`、`en-US.ts`（`managedKeyBadge`）

测试：
- 修改/新增 `mateclaw-server/src/test/java/vip/mate/llm/service/ModelProviderServiceManagedDefaultTest.java`

---

## Task 1: 后端 — 受管集合 + `managedKey` DTO 标记 + `defaultProviderKey` 重映射

**Files:**
- Modify: `mateclaw-server/src/main/java/vip/mate/llm/model/ProviderInfoDTO.java`
- Modify: `mateclaw-server/src/main/java/vip/mate/llm/service/ModelProviderService.java`
- Test: `mateclaw-server/src/test/java/vip/mate/llm/service/ModelProviderServiceManagedDefaultTest.java`

- [ ] **Step 1: 给 DTO 加 `managedKey` 字段**

在 `ProviderInfoDTO.java` 的 `enabled` 字段下方加入：

```java
    /** RFC-074: whether the user has explicitly opted this provider into the dropdown. */
    private Boolean enabled;

    /**
     * 平台托管的「默认版」provider（dashscope-default 等）：其 api_key 由配置文件
     * 注入，前端据此隐藏/禁用 key 输入并隐藏删除。普通 provider 为 false/null。
     */
    private Boolean managedKey;
```

- [ ] **Step 2: 写失败测试（managedKey 标记）**

新建 `ModelProviderServiceManagedDefaultTest.java`（构造方式照搬 `ModelProviderServiceConfiguredTest` 的 `setUp`）：

```java
package vip.mate.llm.service;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.ArgumentCaptor;
import org.springframework.beans.factory.ObjectProvider;
import org.springframework.context.ApplicationEventPublisher;
import vip.mate.llm.anthropic.oauth.ClaudeCodeOAuthService;
import vip.mate.llm.config.DefaultProviderKeyProperties;
import vip.mate.llm.event.ModelConfigChangedEvent;
import vip.mate.llm.failover.AvailableProviderPool;
import vip.mate.llm.failover.ProviderHealthProperties;
import vip.mate.llm.failover.ProviderHealthTracker;
import vip.mate.llm.failover.ProviderInitProbe;
import vip.mate.llm.model.ModelConfigEntity;
import vip.mate.llm.model.ModelProviderEntity;
import vip.mate.llm.model.ProviderConfigRequest;
import vip.mate.llm.model.ProviderInfoDTO;
import vip.mate.llm.repository.ModelProviderMapper;

import java.util.List;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.*;

class ModelProviderServiceManagedDefaultTest {

    private ModelProviderMapper providerMapper;
    private ModelConfigService modelConfigService;
    private ApplicationEventPublisher eventPublisher;
    private AvailableProviderPool pool;
    private ProviderHealthTracker healthTracker;
    private ProviderInitProbe initProbe;
    private DefaultProviderKeyProperties keyProps;
    private ModelProviderService service;

    @BeforeEach
    @SuppressWarnings("unchecked")
    void setUp() {
        providerMapper = mock(ModelProviderMapper.class);
        modelConfigService = mock(ModelConfigService.class);
        eventPublisher = mock(ApplicationEventPublisher.class);
        ObjectProvider<ClaudeCodeOAuthService> claudeProvider = mock(ObjectProvider.class);
        when(claudeProvider.getIfAvailable()).thenReturn(null);
        pool = new AvailableProviderPool();
        ProviderHealthProperties props = new ProviderHealthProperties();
        props.setFailureThreshold(1);
        healthTracker = new ProviderHealthTracker(props);
        initProbe = mock(ProviderInitProbe.class);
        ObjectProvider<ProviderInitProbe> initProbeProvider = mock(ObjectProvider.class);
        when(initProbeProvider.getIfAvailable()).thenReturn(initProbe);
        when(initProbe.hasBeenProbed(any())).thenReturn(true);
        keyProps = new DefaultProviderKeyProperties();
        keyProps.setDashscope("sk-platform-dash");
        keyProps.setDeepseek("sk-platform-deep");
        service = new ModelProviderService(providerMapper, modelConfigService, eventPublisher,
                claudeProvider, pool, healthTracker, keyProps,
                mock(ProviderTokenQuotaService.class), initProbeProvider);
    }

    private ModelProviderEntity provider(String id) {
        ModelProviderEntity p = new ModelProviderEntity();
        p.setProviderId(id);
        p.setName(id);
        p.setIsLocal(false);
        p.setIsCustom(false);
        p.setRequireApiKey(true);
        p.setEnabled(true);
        p.setApiKey("sk-real-key-1234567890");
        return p;
    }

    @Test
    void managedDefaultProviderIsFlaggedManagedKey() {
        when(providerMapper.selectList(any(LambdaQueryWrapper.class)))
                .thenReturn(List.of(provider("dashscope-default")));
        when(modelConfigService.listModels()).thenReturn(List.of());
        ProviderInfoDTO dto = service.listProviders().get(0);
        assertEquals(Boolean.TRUE, dto.getManagedKey());
    }

    @Test
    void plainProviderIsNotManagedKey() {
        when(providerMapper.selectList(any(LambdaQueryWrapper.class)))
                .thenReturn(List.of(provider("dashscope")));
        when(modelConfigService.listModels()).thenReturn(List.of());
        ProviderInfoDTO dto = service.listProviders().get(0);
        assertNotEquals(Boolean.TRUE, dto.getManagedKey());
    }
}
```

- [ ] **Step 3: 运行测试确认失败**

Run: `cd mateclaw-server && JAVA_HOME=$(/usr/libexec/java_home -v 21) mvn -q -Dtest=ModelProviderServiceManagedDefaultTest test`
Expected: 编译失败或断言失败（`getManagedKey` 未赋值 / 方法不存在）。

- [ ] **Step 4: 实现受管集合 + 标记**

在 `ModelProviderService` 类里，`PROVIDER_ID_PATTERN` 常量下方新增：

```java
    /**
     * 平台托管的「默认版」云端 provider：api_key 由 {@link DefaultProviderKeyProperties}
     * 注入，UI 上只读、不可删除。新增/调整时与 {@link #managedDefaultKeyFor} 同步。
     */
    static final java.util.Set<String> MANAGED_DEFAULT_PROVIDER_IDS = java.util.Set.of(
            "dashscope-default", "dashscope-compat-default", "deepseek-default");

    static boolean isManagedDefaultProvider(String providerId) {
        return providerId != null && MANAGED_DEFAULT_PROVIDER_IDS.contains(providerId);
    }
```

在 `toProviderInfo(...)` 中、`dto.setEnabled(...)` 一行附近加入：

```java
        dto.setEnabled(Boolean.TRUE.equals(provider.getEnabled()));
        dto.setManagedKey(isManagedDefaultProvider(provider.getProviderId()));
```

- [ ] **Step 5: 重映射 `defaultProviderKey` 到新 id**

把现有 `defaultProviderKey(String providerId)` 方法体替换为（并新增公有 `managedDefaultKeyFor`，供启动注入复用）：

```java
    private String defaultProviderKey(String providerId) {
        return managedDefaultKeyFor(providerId);
    }

    /**
     * 受管「默认版」provider 的平台 key（来自配置文件）；非受管或未配置返回 null。
     * 两个 dashscope 默认版共用 dashscope 字段，deepseek 默认版用 deepseek 字段。
     */
    public String managedDefaultKeyFor(String providerId) {
        if (defaultProviderKeyProperties == null || providerId == null) {
            return null;
        }
        return switch (providerId) {
            case "dashscope-default", "dashscope-compat-default" -> defaultProviderKeyProperties.getDashscope();
            case "deepseek-default" -> defaultProviderKeyProperties.getDeepseek();
            default -> null;
        };
    }
```

> 注意：旧 switch 里的 `case "dashscope"` / `case "deepseek"` 必须删除——原版不再自动获得平台 key。

- [ ] **Step 6: 运行测试确认通过**

Run: `cd mateclaw-server && JAVA_HOME=$(/usr/libexec/java_home -v 21) mvn -q -Dtest=ModelProviderServiceManagedDefaultTest test`
Expected: PASS（两个用例）。

- [ ] **Step 7: 提交**

```bash
git add mateclaw-server/src/main/java/vip/mate/llm/model/ProviderInfoDTO.java \
        mateclaw-server/src/main/java/vip/mate/llm/service/ModelProviderService.java \
        mateclaw-server/src/test/java/vip/mate/llm/service/ModelProviderServiceManagedDefaultTest.java
git commit -m "feat(llm): managed default provider 集合 + managedKey 标记 + defaultProviderKey 重映射"
```

---

## Task 2: 后端 — `updateProviderConfig` 对受管 provider 只读守卫

**Files:**
- Modify: `mateclaw-server/src/main/java/vip/mate/llm/service/ModelProviderService.java:129-150`
- Test: `ModelProviderServiceManagedDefaultTest.java`（追加用例）

- [ ] **Step 1: 追加失败测试**

在 `ModelProviderServiceManagedDefaultTest` 中追加：

```java
    @Test
    void updateProviderConfigIgnoresKeyAndBaseUrlForManaged() {
        ModelProviderEntity managed = provider("deepseek-default");
        managed.setApiKey("sk-platform-deep");
        managed.setBaseUrl("https://api.deepseek.com");
        when(providerMapper.selectOne(any(LambdaQueryWrapper.class))).thenReturn(managed);
        when(modelConfigService.listModelsByProvider(any())).thenReturn(List.of());

        ProviderConfigRequest req = new ProviderConfigRequest();
        req.setApiKey("sk-HACKED");
        req.setBaseUrl("https://evil.example.com");
        req.setProtocol("openai-compatible");

        service.updateProviderConfig("deepseek-default", req);

        ArgumentCaptor<ModelProviderEntity> captor = ArgumentCaptor.forClass(ModelProviderEntity.class);
        verify(providerMapper).updateById(captor.capture());
        assertEquals("sk-platform-deep", captor.getValue().getApiKey(), "受管 provider 的 key 不可被覆盖");
        assertEquals("https://api.deepseek.com", captor.getValue().getBaseUrl(), "受管 provider 的 baseUrl 不可被覆盖");
    }

    @Test
    void updateProviderConfigStillWritesKeyForPlainProvider() {
        ModelProviderEntity plain = provider("deepseek");
        plain.setApiKey("");
        when(providerMapper.selectOne(any(LambdaQueryWrapper.class))).thenReturn(plain);
        when(modelConfigService.listModelsByProvider(any())).thenReturn(List.of());

        ProviderConfigRequest req = new ProviderConfigRequest();
        req.setApiKey("sk-user-own-key-1234");
        req.setBaseUrl("https://api.deepseek.com");
        req.setProtocol("openai-compatible");

        service.updateProviderConfig("deepseek", req);

        ArgumentCaptor<ModelProviderEntity> captor = ArgumentCaptor.forClass(ModelProviderEntity.class);
        verify(providerMapper).updateById(captor.capture());
        assertEquals("sk-user-own-key-1234", captor.getValue().getApiKey());
    }
```

> 若 `ProviderConfigRequest` 的 setter 名与上面不符，先 `Read` 该类对齐字段名。

- [ ] **Step 2: 运行确认失败**

Run: `cd mateclaw-server && JAVA_HOME=$(/usr/libexec/java_home -v 21) mvn -q -Dtest=ModelProviderServiceManagedDefaultTest test`
Expected: `updateProviderConfigIgnoresKeyAndBaseUrlForManaged` 失败（key/baseUrl 被覆盖）。

- [ ] **Step 3: 加只读守卫**

把 `updateProviderConfig` 开头的 key/baseUrl 赋值改为受管判定：

```java
    public ProviderInfoDTO updateProviderConfig(String providerId, ProviderConfigRequest request) {
        ModelProviderEntity provider = getProvider(providerId);
        boolean managed = isManagedDefaultProvider(providerId);
        // 受管「默认版」：平台 key / baseUrl 只读，静默忽略外部改动（UI 已禁用，此处为纵深防御）。
        if (!managed && StringUtils.hasText(request.getApiKey())) {
            provider.setApiKey(request.getApiKey().trim());
        }
        if (!managed) {
            provider.setBaseUrl(request.getBaseUrl());
        }
        provider.setChatModel(ModelProtocol.resolveChatModel(request.getProtocol(), request.getChatModel()));
        provider.setGenerateKwargs(writeJson(request.getGenerateKwargs()));
        // ...（其余保持不变）
```

其余行（requireApiKey、fallbackPriority、updateById、tryAutoActivateModel、event、return）保持原样。

- [ ] **Step 4: 运行确认通过**

Run: `cd mateclaw-server && JAVA_HOME=$(/usr/libexec/java_home -v 21) mvn -q -Dtest=ModelProviderServiceManagedDefaultTest test`
Expected: PASS（全部用例）。

- [ ] **Step 5: 提交**

```bash
git add mateclaw-server/src/main/java/vip/mate/llm/service/ModelProviderService.java \
        mateclaw-server/src/test/java/vip/mate/llm/service/ModelProviderServiceManagedDefaultTest.java
git commit -m "feat(llm): updateProviderConfig 对受管默认 provider 只读保护 key/baseUrl"
```

---

## Task 3: 后端 — 平台 key 启动注入（全工作区，幂等）

**Files:**
- Modify: `mateclaw-server/src/main/java/vip/mate/llm/service/ModelProviderService.java`
- Create: `mateclaw-server/src/main/java/vip/mate/llm/config/DefaultProviderKeyBootstrap.java`
- Modify: `mateclaw-server/src/main/java/vip/mate/system/controller/SetupController.java`
- Test: `ModelProviderServiceManagedDefaultTest.java`（追加用例）

- [ ] **Step 1: 追加失败测试**

```java
    @Test
    void applyManagedDefaultKeysFillsEmptyKeyAndEnables() {
        ModelProviderEntity row = provider("dashscope-default");
        row.setApiKey("");          // 空 key
        row.setEnabled(false);
        when(providerMapper.selectList(any(LambdaQueryWrapper.class))).thenReturn(List.of(row));

        service.applyManagedDefaultProviderKeys();

        ArgumentCaptor<ModelProviderEntity> captor = ArgumentCaptor.forClass(ModelProviderEntity.class);
        verify(providerMapper).updateById(captor.capture());
        assertEquals("sk-platform-dash", captor.getValue().getApiKey());
        assertEquals(Boolean.TRUE, captor.getValue().getEnabled());
        verify(eventPublisher).publishEvent(any(ModelConfigChangedEvent.class));
    }

    @Test
    void applyManagedDefaultKeysSkipsWhenAlreadyConfigured() {
        ModelProviderEntity row = provider("dashscope-default");
        row.setApiKey("sk-already-set-1234567890");
        when(providerMapper.selectList(any(LambdaQueryWrapper.class))).thenReturn(List.of(row));

        service.applyManagedDefaultProviderKeys();

        verify(providerMapper, never()).updateById(any());
    }

    @Test
    void applyManagedDefaultKeysSkipsWhenConfigKeyBlank() {
        keyProps.setDashscope("");   // 平台没配 key
        ModelProviderEntity row = provider("dashscope-default");
        row.setApiKey("");
        when(providerMapper.selectList(any(LambdaQueryWrapper.class))).thenReturn(List.of(row));

        service.applyManagedDefaultProviderKeys();

        verify(providerMapper, never()).updateById(any());
    }
```

- [ ] **Step 2: 运行确认失败**

Run: `cd mateclaw-server && JAVA_HOME=$(/usr/libexec/java_home -v 21) mvn -q -Dtest=ModelProviderServiceManagedDefaultTest test`
Expected: 编译失败（`applyManagedDefaultProviderKeys` 不存在）。

- [ ] **Step 3: 实现 `applyManagedDefaultProviderKeys()`**

在 `ModelProviderService` 中新增（放在 `applyRegistrationDefaultProviderKey` 附近）：

```java
    /**
     * 对所有工作区里 {@link #MANAGED_DEFAULT_PROVIDER_IDS} 的 provider：当 api_key 为空/占位
     * 且配置文件提供了平台 key 时，回填该 key 并置 enabled=TRUE。幂等，无请求上下文依赖
     * （按 PK 更新，不经工作区 resolver）。web 模式由 {@code DefaultProviderKeyBootstrap}
     * 在启动后调用；桌面延迟 seed 模式由 {@code SetupController.init} 调用。
     */
    public void applyManagedDefaultProviderKeys() {
        List<ModelProviderEntity> rows = modelProviderMapper.selectList(
                new LambdaQueryWrapper<ModelProviderEntity>()
                        .in(ModelProviderEntity::getProviderId, MANAGED_DEFAULT_PROVIDER_IDS));
        boolean changed = false;
        for (ModelProviderEntity row : rows) {
            String key = managedDefaultKeyFor(row.getProviderId());
            if (StringUtils.hasText(key) && !hasUsableApiKey(row.getApiKey())) {
                row.setApiKey(key.trim());
                row.setEnabled(true);
                modelProviderMapper.updateById(row);
                changed = true;
            }
        }
        if (changed) {
            eventPublisher.publishEvent(new ModelConfigChangedEvent("managed-default-keys-applied"));
        }
    }
```

- [ ] **Step 4: 运行确认通过**

Run: `cd mateclaw-server && JAVA_HOME=$(/usr/libexec/java_home -v 21) mvn -q -Dtest=ModelProviderServiceManagedDefaultTest test`
Expected: PASS（全部用例）。

- [ ] **Step 5: 新建启动 Runner（web/常规启动路径）**

Create `mateclaw-server/src/main/java/vip/mate/llm/config/DefaultProviderKeyBootstrap.java`：

```java
package vip.mate.llm.config;

import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.boot.ApplicationArguments;
import org.springframework.boot.ApplicationRunner;
import org.springframework.core.annotation.Order;
import org.springframework.stereotype.Component;
import vip.mate.llm.service.ModelProviderService;

/**
 * 启动后把配置文件里的平台默认 key 注入受管「默认版」provider。
 * Order 120：晚于 DatabaseBootstrapRunner(@Order(1)) 的种子加载，确保 provider 行已存在。
 * 幂等——空 key 才回填；桌面延迟 seed 由 SetupController 另行触发。
 */
@Slf4j
@Component
@Order(120)
@RequiredArgsConstructor
public class DefaultProviderKeyBootstrap implements ApplicationRunner {

    private final ModelProviderService modelProviderService;

    @Override
    public void run(ApplicationArguments args) {
        try {
            modelProviderService.applyManagedDefaultProviderKeys();
        } catch (Exception e) {
            log.warn("[DefaultProviderKeyBootstrap] 注入平台默认 key 失败，跳过", e);
        }
    }
}
```

- [ ] **Step 6: 桌面延迟 seed 路径也触发注入**

在 `SetupController.init` 中，`initWithLocale` 成功后追加一行：

```java
        boolean success = bootstrapRunner.initWithLocale(language);
        if (!success) {
            throw new ResponseStatusException(HttpStatus.CONFLICT, "Application already initialized");
        }
        // 桌面延迟 seed：provider 行此刻才插入，立即回填平台默认 key。
        modelProviderService.applyManagedDefaultProviderKeys();

        log.info("Application initialized with language={}", language);
```

- [ ] **Step 7: 编译并跑相关测试**

Run: `cd mateclaw-server && JAVA_HOME=$(/usr/libexec/java_home -v 21) mvn -q -Dtest=ModelProviderServiceManagedDefaultTest test`
Expected: PASS；编译通过（新 Runner / SetupController 改动）。

- [ ] **Step 8: 提交**

```bash
git add mateclaw-server/src/main/java/vip/mate/llm/service/ModelProviderService.java \
        mateclaw-server/src/main/java/vip/mate/llm/config/DefaultProviderKeyBootstrap.java \
        mateclaw-server/src/main/java/vip/mate/system/controller/SetupController.java \
        mateclaw-server/src/test/java/vip/mate/llm/service/ModelProviderServiceManagedDefaultTest.java
git commit -m "feat(llm): 启动 + setup/init 后回填平台默认 key 到受管 provider"
```

---

## Task 4: 种子数据 — 全新库新增默认版 provider + 镜像模型 + is_default 迁移

> 4 个文件改动一致，仅 provider 显示名按语言不同。先做 H2 中文版（data-zh.sql）并验证，再镜像其余 3 个。
> 模型镜像 id 采用「源 id + 70000000」固定偏移（确定性、不与现有 1000000xxx / 1.0e18 区块冲突）。

**Files:**
- Modify: `mateclaw-server/src/main/resources/db/data-zh.sql`
- Modify: `mateclaw-server/src/main/resources/db/data-en.sql`
- Modify: `mateclaw-server/src/main/resources/db/data-mysql-zh.sql`
- Modify: `mateclaw-server/src/main/resources/db/data-mysql-en.sql`

- [ ] **Step 1: data-zh.sql 新增三个默认版 provider**

在原 `deepseek` 的 `MERGE INTO mate_model_provider ... VALUES ('deepseek', ...)` 之后追加：

```sql
-- ==================== 默认版 Provider（平台预置 key，开箱即用）====================
-- api_key 留空，由 DefaultProviderKeyBootstrap / SetupController 从配置文件注入；
-- enabled 默认 FALSE，注入 key 时一并置 TRUE。support_model_discovery=FALSE 锁定模型集。
MERGE INTO mate_model_provider (provider_id, name, api_key_prefix, chat_model, api_key, base_url, generate_kwargs, is_custom, is_local, support_model_discovery, support_connection_check, freeze_url, require_api_key, create_time, update_time)
KEY (provider_id)
VALUES ('dashscope-default', 'DashScope 默认', 'sk-', 'DashScopeChatModel', '', '', '{}', FALSE, FALSE, FALSE, TRUE, TRUE, TRUE, NOW(), NOW());

MERGE INTO mate_model_provider (provider_id, name, api_key_prefix, chat_model, api_key, base_url, generate_kwargs, is_custom, is_local, support_model_discovery, support_connection_check, freeze_url, require_api_key, create_time, update_time)
KEY (provider_id)
VALUES ('dashscope-compat-default', 'DashScope 兼容模式 默认', 'sk-', 'OpenAIChatModel', '', 'https://dashscope.aliyuncs.com/compatible-mode/v1', '{}', FALSE, FALSE, FALSE, TRUE, TRUE, TRUE, NOW(), NOW());

MERGE INTO mate_model_provider (provider_id, name, api_key_prefix, chat_model, api_key, base_url, generate_kwargs, is_custom, is_local, support_model_discovery, support_connection_check, freeze_url, require_api_key, create_time, update_time)
KEY (provider_id)
VALUES ('deepseek-default', 'DeepSeek 默认', 'sk-', 'OpenAIChatModel', '', 'https://api.deepseek.com', '{}', FALSE, FALSE, FALSE, TRUE, TRUE, TRUE, NOW(), NOW());
```

- [ ] **Step 2: data-zh.sql 末尾（所有原版模型 INSERT 之后）镜像模型 + 迁移默认模型**

在所有 `mate_model_config` 原版模型行之后追加：

```sql
-- ==================== 默认版模型（镜像原版 builtin 模型，id = 源 id + 70000000）====================
INSERT INTO mate_model_config (id, name, provider, model_name, description, temperature, max_tokens, top_p, builtin, enabled, is_default, create_time, update_time, deleted)
SELECT id + 70000000, name, 'dashscope-default', model_name, description, temperature, max_tokens, top_p, builtin, enabled, FALSE, create_time, update_time, deleted
  FROM mate_model_config
 WHERE provider = 'dashscope' AND builtin = TRUE AND deleted = 0;

INSERT INTO mate_model_config (id, name, provider, model_name, description, temperature, max_tokens, top_p, builtin, enabled, is_default, create_time, update_time, deleted)
SELECT id + 70000000, name, 'dashscope-compat-default', model_name, description, temperature, max_tokens, top_p, builtin, enabled, FALSE, create_time, update_time, deleted
  FROM mate_model_config
 WHERE provider = 'dashscope-compat' AND builtin = TRUE AND deleted = 0;

INSERT INTO mate_model_config (id, name, provider, model_name, description, temperature, max_tokens, top_p, builtin, enabled, is_default, create_time, update_time, deleted)
SELECT id + 70000000, name, 'deepseek-default', model_name, description, temperature, max_tokens, top_p, builtin, enabled, FALSE, create_time, update_time, deleted
  FROM mate_model_config
 WHERE provider = 'deepseek' AND builtin = TRUE AND deleted = 0;

-- 开箱默认聊天模型从原版 dashscope/qwen-plus 迁到 dashscope-default/qwen-plus（保证全局唯一）
UPDATE mate_model_config SET is_default = FALSE WHERE provider = 'dashscope' AND model_name = 'qwen-plus';
UPDATE mate_model_config SET is_default = TRUE  WHERE provider = 'dashscope-default' AND model_name = 'qwen-plus';
```

- [ ] **Step 3: H2 全新库冒烟验证（脚本能跑通 + 结果正确）**

启动一次后端（H2 内存/文件库为全新），或用现有集成测试触发种子加载。最简校验：启动后调用接口确认默认版存在且唯一默认模型。

Run（启动后端，dev profile，全新 H2）：
```bash
cd mateclaw-server && JAVA_HOME=$(/usr/libexec/java_home -v 21) mvn -q spring-boot:run
```
另开终端验证：
```bash
curl -s localhost:8080/api/v1/setup/onboarding-status   # configuredProviders 应含 dashscope-default
```
Expected: `dashscope-default`（及兼容/deepseek 默认版，若平台 key 已配）出现在 `configuredProviders`；无报错。停止应用。

> 若不便启动，改用 H2 命令行对 `data-zh.sql` 跑一遍并 `SELECT provider, COUNT(*), SUM(CASE WHEN is_default THEN 1 ELSE 0 END) FROM mate_model_config GROUP BY provider;` 确认 `is_default` 合计为 1。

- [ ] **Step 4: 镜像到 data-en.sql（英文名）**

同 Step 1/2，但 provider 名用英文：`'DashScope (Default)'`、`'DashScope Compatible (Default)'`、`'DeepSeek (Default)'`。模型镜像/默认迁移 SQL 完全一致。

- [ ] **Step 5: 镜像到 data-mysql-zh.sql（MySQL 语法）**

provider 用 `INSERT ... ON DUPLICATE KEY UPDATE` 形式（照搬该文件里 `deepseek` 行的写法），例如：

```sql
INSERT INTO mate_model_provider (provider_id, name, api_key_prefix, chat_model, api_key, base_url, generate_kwargs, is_custom, is_local, support_model_discovery, support_connection_check, freeze_url, require_api_key, create_time, update_time)
VALUES ('dashscope-default', 'DashScope 默认', 'sk-', 'DashScopeChatModel', '', '', '{}', FALSE, FALSE, FALSE, TRUE, TRUE, TRUE, NOW(), NOW())
ON DUPLICATE KEY UPDATE name=VALUES(name), api_key_prefix=VALUES(api_key_prefix), chat_model=VALUES(chat_model), base_url=VALUES(base_url), generate_kwargs=VALUES(generate_kwargs), is_custom=VALUES(is_custom), is_local=VALUES(is_local), support_model_discovery=VALUES(support_model_discovery), support_connection_check=VALUES(support_connection_check), freeze_url=VALUES(freeze_url), require_api_key=VALUES(require_api_key), update_time=VALUES(update_time);
```

（compat-default / deepseek-default 同形，base_url 分别为 compat 端点 / `https://api.deepseek.com`。）

模型镜像与默认迁移：MySQL 8 同样支持 `INSERT ... SELECT` 与窗口；三条镜像 INSERT 与两条 UPDATE 与 H2 写法一致（MySQL 允许 `INSERT INTO t SELECT ... FROM t`）。直接复用 Step 2 的 SQL。

- [ ] **Step 6: 镜像到 data-mysql-en.sql（MySQL + 英文名）**

同 Step 5，provider 名用英文（同 Step 4）。

- [ ] **Step 7: 提交**

```bash
git add mateclaw-server/src/main/resources/db/data-zh.sql \
        mateclaw-server/src/main/resources/db/data-en.sql \
        mateclaw-server/src/main/resources/db/data-mysql-zh.sql \
        mateclaw-server/src/main/resources/db/data-mysql-en.sql
git commit -m "feat(llm): 种子数据新增 DashScope/DeepSeek 默认版 provider + 镜像模型 + 默认模型迁移"
```

---

## Task 5: Flyway 迁移 V145 — 老库按工作区补齐默认版

> 对所有已存在工作区幂等地补三个默认版 provider + 镜像模型，并条件迁移 is_default（仅当原版未配置且当前默认仍是原版 qwen-plus）。
> id 用高位区块 + `ROW_NUMBER()`（照搬 V133 风格）。**provider 行 enabled 插入为 FALSE，平台 key 由本次启动的 DefaultProviderKeyBootstrap 回填并置 TRUE。**
> H2 与 MySQL 的差异点：MySQL 不允许「UPDATE 表的同时在子查询里 SELECT 同表」，相关 UPDATE 必须用派生表包一层。

**Files:**
- Create: `mateclaw-server/src/main/resources/db/migration/h2/V145__seed_default_cloud_provider_keys.sql`
- Create: `mateclaw-server/src/main/resources/db/migration/mysql/V145__seed_default_cloud_provider_keys.sql`

- [ ] **Step 1: 实现 H2 版 V145**

> 实现前先确认 id 区块未冲突：`grep -rn "90[0-9]000000000000000" mateclaw-server/src/main/resources/db/migration/`，确保 `9071..9076 e17` 这几个区块未被 V133–V144 占用；若有冲突，整体上移到一个空闲区块。

```sql
-- V145: 新增 DashScope/DeepSeek「默认版」provider（平台预置 key），对所有工作区补齐。
-- api_key 留空、enabled=FALSE，由 DefaultProviderKeyBootstrap 启动后回填配置文件里的平台 key。

-- ---- 默认版 provider（每工作区一份，幂等）----
INSERT INTO mate_model_provider (id, workspace_id, provider_id, name, api_key_prefix, chat_model, api_key, base_url, generate_kwargs, is_custom, is_local, support_model_discovery, support_connection_check, freeze_url, require_api_key, auth_type, fallback_priority, enabled, create_time, update_time)
SELECT 907100000000000000 + ROW_NUMBER() OVER (ORDER BY w.id),
       w.id, 'dashscope-default', 'DashScope 默认', 'sk-', 'DashScopeChatModel', '', '', '{}',
       FALSE, FALSE, FALSE, TRUE, TRUE, TRUE, 'api_key', 0, FALSE, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
  FROM mate_workspace w
 WHERE NOT EXISTS (SELECT 1 FROM mate_model_provider p WHERE p.workspace_id = w.id AND p.provider_id = 'dashscope-default');

INSERT INTO mate_model_provider (id, workspace_id, provider_id, name, api_key_prefix, chat_model, api_key, base_url, generate_kwargs, is_custom, is_local, support_model_discovery, support_connection_check, freeze_url, require_api_key, auth_type, fallback_priority, enabled, create_time, update_time)
SELECT 907200000000000000 + ROW_NUMBER() OVER (ORDER BY w.id),
       w.id, 'dashscope-compat-default', 'DashScope 兼容模式 默认', 'sk-', 'OpenAIChatModel', '', 'https://dashscope.aliyuncs.com/compatible-mode/v1', '{}',
       FALSE, FALSE, FALSE, TRUE, TRUE, TRUE, 'api_key', 0, FALSE, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
  FROM mate_workspace w
 WHERE NOT EXISTS (SELECT 1 FROM mate_model_provider p WHERE p.workspace_id = w.id AND p.provider_id = 'dashscope-compat-default');

INSERT INTO mate_model_provider (id, workspace_id, provider_id, name, api_key_prefix, chat_model, api_key, base_url, generate_kwargs, is_custom, is_local, support_model_discovery, support_connection_check, freeze_url, require_api_key, auth_type, fallback_priority, enabled, create_time, update_time)
SELECT 907300000000000000 + ROW_NUMBER() OVER (ORDER BY w.id),
       w.id, 'deepseek-default', 'DeepSeek 默认', 'sk-', 'OpenAIChatModel', '', 'https://api.deepseek.com', '{}',
       FALSE, FALSE, FALSE, TRUE, TRUE, TRUE, 'api_key', 0, FALSE, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
  FROM mate_workspace w
 WHERE NOT EXISTS (SELECT 1 FROM mate_model_provider p WHERE p.workspace_id = w.id AND p.provider_id = 'deepseek-default');

-- ---- 镜像 builtin 模型（同工作区内复制原版 → 默认版，幂等）----
INSERT INTO mate_model_config (id, workspace_id, name, provider, model_name, description, temperature, max_tokens, top_p, builtin, enabled, is_default, max_input_tokens, enable_search, search_strategy, model_type, modalities, request_timeout_seconds, create_time, update_time, deleted)
SELECT 907400000000000000 + ROW_NUMBER() OVER (ORDER BY m.workspace_id, m.id),
       m.workspace_id, m.name, 'dashscope-default', m.model_name, m.description, m.temperature, m.max_tokens, m.top_p,
       m.builtin, m.enabled, FALSE, m.max_input_tokens, m.enable_search, m.search_strategy, m.model_type, m.modalities, m.request_timeout_seconds,
       CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, m.deleted
  FROM mate_model_config m
 WHERE m.provider = 'dashscope' AND m.builtin = TRUE AND m.deleted = 0 AND (m.model_type = 'chat' OR m.model_type IS NULL)
   AND NOT EXISTS (SELECT 1 FROM mate_model_config e WHERE e.workspace_id = m.workspace_id AND e.provider = 'dashscope-default' AND e.model_name = m.model_name AND e.deleted = 0);

INSERT INTO mate_model_config (id, workspace_id, name, provider, model_name, description, temperature, max_tokens, top_p, builtin, enabled, is_default, max_input_tokens, enable_search, search_strategy, model_type, modalities, request_timeout_seconds, create_time, update_time, deleted)
SELECT 907500000000000000 + ROW_NUMBER() OVER (ORDER BY m.workspace_id, m.id),
       m.workspace_id, m.name, 'dashscope-compat-default', m.model_name, m.description, m.temperature, m.max_tokens, m.top_p,
       m.builtin, m.enabled, FALSE, m.max_input_tokens, m.enable_search, m.search_strategy, m.model_type, m.modalities, m.request_timeout_seconds,
       CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, m.deleted
  FROM mate_model_config m
 WHERE m.provider = 'dashscope-compat' AND m.builtin = TRUE AND m.deleted = 0 AND (m.model_type = 'chat' OR m.model_type IS NULL)
   AND NOT EXISTS (SELECT 1 FROM mate_model_config e WHERE e.workspace_id = m.workspace_id AND e.provider = 'dashscope-compat-default' AND e.model_name = m.model_name AND e.deleted = 0);

INSERT INTO mate_model_config (id, workspace_id, name, provider, model_name, description, temperature, max_tokens, top_p, builtin, enabled, is_default, max_input_tokens, enable_search, search_strategy, model_type, modalities, request_timeout_seconds, create_time, update_time, deleted)
SELECT 907600000000000000 + ROW_NUMBER() OVER (ORDER BY m.workspace_id, m.id),
       m.workspace_id, m.name, 'deepseek-default', m.model_name, m.description, m.temperature, m.max_tokens, m.top_p,
       m.builtin, m.enabled, FALSE, m.max_input_tokens, m.enable_search, m.search_strategy, m.model_type, m.modalities, m.request_timeout_seconds,
       CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, m.deleted
  FROM mate_model_config m
 WHERE m.provider = 'deepseek' AND m.builtin = TRUE AND m.deleted = 0 AND (m.model_type = 'chat' OR m.model_type IS NULL)
   AND NOT EXISTS (SELECT 1 FROM mate_model_config e WHERE e.workspace_id = m.workspace_id AND e.provider = 'deepseek-default' AND e.model_name = m.model_name AND e.deleted = 0);

-- ---- 条件迁移默认模型：仅当某工作区当前默认仍是「未配置的原版」dashscope/qwen-plus ----
UPDATE mate_model_config
   SET is_default = TRUE
 WHERE provider = 'dashscope-default' AND model_name = 'qwen-plus'
   AND workspace_id IN (
       SELECT d.workspace_id FROM mate_model_config d
       JOIN mate_model_provider op ON op.workspace_id = d.workspace_id AND op.provider_id = 'dashscope'
       WHERE d.is_default = TRUE AND d.provider = 'dashscope' AND d.model_name = 'qwen-plus'
         AND (op.api_key IS NULL OR TRIM(op.api_key) = '' OR op.api_key LIKE '%*%' OR LOWER(op.api_key) = 'configure-in-admin-ui')
   );

UPDATE mate_model_config
   SET is_default = FALSE
 WHERE provider = 'dashscope' AND model_name = 'qwen-plus' AND is_default = TRUE
   AND workspace_id IN (
       SELECT x.workspace_id FROM mate_model_config x
       WHERE x.provider = 'dashscope-default' AND x.model_name = 'qwen-plus' AND x.is_default = TRUE
   );
```

- [ ] **Step 2: 实现 MySQL 版 V145**

与 H2 版一致，但**两条 is_default UPDATE 用派生表包子查询**（MySQL 1093 限制）：

```sql
-- ...（前面三条 provider INSERT、三条模型镜像 INSERT 与 H2 版完全相同，可直接复用）...

UPDATE mate_model_config
   SET is_default = TRUE
 WHERE provider = 'dashscope-default' AND model_name = 'qwen-plus'
   AND workspace_id IN (
       SELECT ws FROM (
           SELECT d.workspace_id AS ws FROM mate_model_config d
           JOIN mate_model_provider op ON op.workspace_id = d.workspace_id AND op.provider_id = 'dashscope'
           WHERE d.is_default = TRUE AND d.provider = 'dashscope' AND d.model_name = 'qwen-plus'
             AND (op.api_key IS NULL OR TRIM(op.api_key) = '' OR op.api_key LIKE '%*%' OR LOWER(op.api_key) = 'configure-in-admin-ui')
       ) t
   );

UPDATE mate_model_config
   SET is_default = FALSE
 WHERE provider = 'dashscope' AND model_name = 'qwen-plus' AND is_default = TRUE
   AND workspace_id IN (
       SELECT ws FROM (
           SELECT x.workspace_id AS ws FROM mate_model_config x
           WHERE x.provider = 'dashscope-default' AND x.model_name = 'qwen-plus' AND x.is_default = TRUE
       ) t
   );
```

> 实现前用 `Read` 打开 `db/migration/h2/V133__workspace_model_isolation.sql` 与 `db/migration/mysql/V133__workspace_model_isolation.sql` 对照确认：(a) `mate_model_config` 列集合是否已有本计划引用之外的新列（有则一并加入镜像 INSERT），(b) MySQL 端 `mate_model_provider.id` 是否允许显式赋值（V133 mysql 若也显式赋 id 即可照用）。

- [ ] **Step 3: 在「已有数据」的库上验证升级路径**

构造一个已初始化的 H2 库（先用旧代码或先跑一次种子），再用本分支启动触发 Flyway V145。验证：
- 三个默认版 provider 在每个工作区各一行；
- `SELECT workspace_id, SUM(CASE WHEN is_default THEN 1 ELSE 0 END) FROM mate_model_config GROUP BY workspace_id;` 每个工作区合计 = 1；
- 重复启动（再次触发 Flyway 校验/无重复插入）不报错、无重复行。

Run（示例，本地 H2 文件库）：
```bash
cd mateclaw-server && JAVA_HOME=$(/usr/libexec/java_home -v 21) mvn -q spring-boot:run
```
Expected: 启动日志 Flyway 应用 V145 成功；上述 SQL 校验通过。

- [ ] **Step 4: 提交**

```bash
git add mateclaw-server/src/main/resources/db/migration/h2/V145__seed_default_cloud_provider_keys.sql \
        mateclaw-server/src/main/resources/db/migration/mysql/V145__seed_default_cloud_provider_keys.sql
git commit -m "feat(llm): V145 老库按工作区补齐 DashScope/DeepSeek 默认版 provider 与模型"
```

---

## Task 6: 前端 — `managedKey` 类型 + 卡片锁定 + 弹窗禁用 + i18n

**Files:**
- Modify: `mateclaw-ui/src/types/index.ts:874`
- Modify: `mateclaw-ui/src/views/Settings/Models/ProviderCard.vue:60`
- Modify: `mateclaw-ui/src/views/Settings/Models/modals/ProviderConfigModal.vue:25,80`
- Modify: `mateclaw-ui/src/i18n/locales/zh-CN.ts:642`、`mateclaw-ui/src/i18n/locales/en-US.ts:762`

- [ ] **Step 1: 类型加 `managedKey`**

在 `ProviderInfo` 接口 `enabled?: boolean` 之后加入：

```ts
  /** RFC-074: whether the user has explicitly opted this provider into the dropdown. */
  enabled?: boolean
  /** 平台托管的默认版 provider：key 由平台预置，前端只读、不可删除。 */
  managedKey?: boolean
```

- [ ] **Step 2: i18n 文案**

`zh-CN.ts` 在 `inlineApiKeyMasked` 同级加：

```ts
      inlineApiKeyMasked: '已设置 ••••••••',
      managedKeyBadge: '由平台提供',
```

`en-US.ts` 同位置加：

```ts
      inlineApiKeyMasked: 'Set ••••••••',
      managedKeyBadge: 'Platform-provided',
```

- [ ] **Step 3: ProviderCard 锁定 key 展示**

把第 60 行的 `<template v-if="provider.authType !== 'oauth' && provider.requireApiKey">` 改成两段：先放受管只读分支，再把原分支改成 `v-else-if`：

```vue
      <!-- 平台托管默认版：key 只读、不可更换 -->
      <template v-if="provider.authType !== 'oauth' && provider.requireApiKey && provider.managedKey">
        <div class="cred-set">
          <svg class="cred-set__check" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="20 6 9 17 4 12"/>
          </svg>
          <span class="cred-set__masked">{{ t('settings.model.inlineApiKeyMasked') }}</span>
          <span class="cred-managed-badge">{{ t('settings.model.managedKeyBadge') }}</span>
        </div>
      </template>
      <!-- API-key flow (non-OAuth providers that need a key) -->
      <template v-else-if="provider.authType !== 'oauth' && provider.requireApiKey">
```

（原 `<template v-if=...>` 后续内容不动，仅这一行改为 `v-else-if`。）

在 `<style scoped>` 末尾加徽标样式：

```css
.cred-managed-badge {
  font-size: 11px;
  padding: 1px 6px;
  border-radius: 4px;
  background: var(--mc-fill-secondary, rgba(127,127,127,0.12));
  color: var(--mc-text-secondary, #888);
}
```

> 删除入口已由 `v-if="provider.isCustom"`（第 202 行）天然隐藏（受管 provider `isCustom=false`），无需改动。

- [ ] **Step 4: ProviderConfigModal 禁用 key/baseUrl 输入**

baseUrl 输入（第 25-29 行）加 `:disabled`：

```vue
            <input
              v-model="form.baseUrl"
              class="form-input mono"
              :placeholder="baseUrlPlaceholder"
              :disabled="!!editingProvider?.managedKey"
            />
```

apiKey 输入（第 80-86 行）加 `:disabled` 与提示：

```vue
            <input
              v-model="form.apiKey"
              type="password"
              class="form-input mono"
              :placeholder="apiKeyPlaceholder"
              :disabled="!!editingProvider?.managedKey"
              autocomplete="off"
            />
            <div class="field-hint">
              {{ editingProvider?.managedKey ? t('settings.model.managedKeyBadge') : t('settings.model.leaveBlankKeep') }}
            </div>
```

- [ ] **Step 5: 前端类型检查/构建**

Run: `cd mateclaw-ui && npm run type-check`（若无该脚本则 `npx vue-tsc --noEmit`）
Expected: 无类型错误。

- [ ] **Step 6: 提交**

```bash
git add mateclaw-ui/src/types/index.ts \
        mateclaw-ui/src/views/Settings/Models/ProviderCard.vue \
        mateclaw-ui/src/views/Settings/Models/modals/ProviderConfigModal.vue \
        mateclaw-ui/src/i18n/locales/zh-CN.ts \
        mateclaw-ui/src/i18n/locales/en-US.ts
git commit -m "feat(ui): 默认版 provider 的 key 只读展示与禁用编辑"
```

---

## Task 7: 端到端验证

**Files:** 无（仅验证）

- [ ] **Step 1: 后端全量测试**

Run: `cd mateclaw-server && JAVA_HOME=$(/usr/libexec/java_home -v 21) mvn -q test`
Expected: 全绿（重点关注 `vip.mate.llm.service.*` 与种子/迁移相关上下文测试）。

- [ ] **Step 2: 全新库启动冒烟**

全新 H2 启动后端，确认：
- 「模型管理 / 云端模型」主网格出现「DashScope 默认 / DashScope 兼容模式 默认 / DeepSeek 默认」三张卡片，均显示已配置（绿勾）+「由平台提供」徽标，无「更换」按钮、无「删除」。
- 原版 DashScope / DeepSeek 在「添加 Provider」抽屉里，空 key，可填自己的 key 后启用。
- 默认聊天模型可直接对话（走 dashscope-default/qwen-plus）。

- [ ] **Step 3: 只读校验（防越权）**

直接调 `PUT /api/v1/models/dashscope-default/config` 传 `apiKey: "sk-x"`，确认返回的 key 仍是平台 key 掩码、DB 未被改写；`DELETE /api/v1/models/custom-providers/dashscope-default` 返回「内置 Provider 不支持删除」。

- [ ] **Step 4: 升级路径冒烟**

用一个已初始化的旧库启动本分支，确认 Flyway V145 应用成功、三个默认版补齐、每工作区唯一默认模型、原版已配置自有 key 的工作区其默认未被抢走。

- [ ] **Step 5: 最终提交（如有零散修正）**

```bash
git add -A && git commit -m "test(llm): 默认版 provider 端到端验证修正" || echo "无额外改动"
```

---

## 验收标准（对照 spec）

- [ ] 三个默认版 provider 存在、开箱启用、key 来自配置文件、env 可覆盖（spec 3.1/3.2）。
- [ ] 原版 `dashscope`/`dashscope-compat`/`deepseek` 空 key、默认不启用、可由用户填自己的 key（spec 3.3）。
- [ ] 每工作区恰好一个 `is_default`；全新库默认走 dashscope-default，升级条件迁移不抢用户已选默认（spec 3.4 / 4）。
- [ ] 受管 provider 的 key 在 UI 只读、有徽标、无更换/删除入口；后端 `updateProviderConfig` 拒改 key/baseUrl（spec 3.5）。
- [ ] 全新库（4 个种子文件）+ 老库（V145 h2/mysql）两条路径都生效（spec 3.6）。
- [ ] 平台 key 未配置时默认版保持未配置、不报错（spec 4）。
- [ ] 非目标未实现：额度限流、embedding 默认版（spec 6）。
