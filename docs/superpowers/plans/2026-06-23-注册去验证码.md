# 注册去短信验证码 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 注册改为"手机号 + 密码（填两次）"直接完成，去掉短信验证码；密码最少 6 位前后端校验；短信模块保留并标 `@Deprecated`。

**Architecture:** 后端从 `AuthService.register()` 移除验证码校验/补偿与对 `VerificationCodeService` 的依赖，新增密码长度校验；`send-register-code` 接口保留标弃用。前端 `Login.vue` 删验证码 UI/逻辑，加确认密码框与"≥6位/两次一致"前端校验；`api`、i18n 同步。

**Tech Stack:** Java 21 + Spring Boot + MyBatis-Plus + JUnit5/Mockito（mateclaw-server）；Vue 3 + TypeScript + vue-i18n + Vitest（mateclaw-ui，pnpm）。

---

## 文件结构

**后端（mateclaw-server）**
- 改 `src/main/java/vip/mate/auth/model/RegisterRequest.java` — 删 `code` 字段
- 改 `src/main/java/vip/mate/auth/service/AuthService.java` — register 去验证码、加密码长度校验、删 VerificationCodeService 依赖
- 改 `src/main/java/vip/mate/auth/controller/AuthController.java` — `sendRegisterCode` 标 `@Deprecated`
- 改 `src/test/java/vip/mate/auth/service/AuthServiceRegisterTest.java` — 删验证码用例、加短密码用例
- 不动：`auth/sms/*`、`AuthControllerSendCodeTest`、`VerificationCodeServiceTest`、`InMemoryVerificationCodeStoreTest`、`AuthControllerRegisterTest`

**前端（mateclaw-ui）**
- 改 `src/api/index.ts` — register 入参去 `code`，删 `sendRegisterCode`
- 改 `src/i18n/locales/zh-CN.ts`、`src/i18n/locales/en-US.ts` — login 段增删键
- 改 `src/views/Login.vue` — 模板 + 脚本 + 失效 CSS

---

## Task 1：后端 — 注册去验证码 + 密码≥6位

**Files:**
- Modify: `mateclaw-server/src/main/java/vip/mate/auth/model/RegisterRequest.java`
- Modify: `mateclaw-server/src/main/java/vip/mate/auth/service/AuthService.java`
- Modify: `mateclaw-server/src/main/java/vip/mate/auth/controller/AuthController.java`
- Test: `mateclaw-server/src/test/java/vip/mate/auth/service/AuthServiceRegisterTest.java`

- [ ] **Step 1: `RegisterRequest` 删除 `code` 字段**

把整个文件替换为：

```java
package vip.mate.auth.model;

import lombok.Data;

/**
 * 手机号注册请求。
 */
@Data
public class RegisterRequest {
    private String phone;
    private String password;
    private String nickname;
}
```

- [ ] **Step 2: `AuthService` 删除 VerificationCodeService 依赖**

删除 import（原第 19 行）：

```java
import vip.mate.auth.sms.VerificationCodeService;
```

删除字段（原第 45 行）：

```java
    private final VerificationCodeService verificationCodeService;
```

- [ ] **Step 3: `AuthService.register()` 改造**

将原 `register` 方法（原第 72-120 行，从 Javadoc `/** 手机号注册：...` 到方法结束 `}`）整体替换为：

```java
    /** 手机号注册：手机号作为 username 保存，密码 BCrypt 加密。 */
    @Transactional
    public LoginResponse register(RegisterRequest request) {
        String phone = PhoneNumbers.normalize(request != null ? request.getPhone() : null);
        if (!PhoneNumbers.isValid(phone)) {
            throw new MateClawException("err.auth.invalid_phone", 400, "手机号格式不正确");
        }
        if (request.getPassword() == null || request.getPassword().isBlank()) {
            throw new MateClawException("err.auth.password_required", 400, "Password is required");
        }
        if (request.getPassword().trim().length() < 6) {
            throw new MateClawException("err.auth.password_too_short", 400, "密码长度至少6位");
        }

        Long count = userMapper.selectCount(new LambdaQueryWrapper<UserEntity>()
                .eq(UserEntity::getUsername, phone));
        if (count > 0) {
            throw new MateClawException("err.auth.username_exists", 409, "手机号已注册: " + phone);
        }

        UserEntity user = new UserEntity();
        user.setUsername(phone);
        user.setPassword(passwordEncoder.encode(request.getPassword().trim()));
        user.setNickname(request.getNickname() == null || request.getNickname().isBlank()
                ? phone
                : request.getNickname().trim());
        user.setRole("user");
        user.setEnabled(true);
        user.setExpiresAt(LocalDateTime.now().plusDays(30));
        try {
            userMapper.insert(user);
        } catch (DuplicateKeyException e) {
            throw new MateClawException("err.auth.username_exists", 409, "手机号已注册: " + phone);
        }

        WorkspaceEntity workspace = new WorkspaceEntity();
        workspace.setName(user.getNickname() + " Workspace");
        WorkspaceEntity createdWorkspace = workspaceService.create(workspace, user.getId());

        String token = generateToken(user);
        return loginResponse(user, token, createdWorkspace != null ? createdWorkspace.getId() : null);
    }
```

（变化：删除 `verifyAndConsume(...)`、删除外层 `try/catch` + `releaseSendLock(...)` 补偿；保留对 `userMapper.insert` 的 `DuplicateKeyException → 409` 内层映射；在密码非空检查后新增 `< 6` 校验。）

- [ ] **Step 4: `AuthController.sendRegisterCode` 标记弃用**

在 `sendRegisterCode` 方法（原第 53 行 `@Operation` 上方）追加 `@Deprecated` 与说明注释，方法体不变：

```java
    @Deprecated // 注册已不再需要验证码，保留以便日后做"可选验证码"开关。
    @Operation(summary = "发送注册验证码")
    @PostMapping("/send-register-code")
    public R<Void> sendRegisterCode(@RequestBody SendCodeRequest request, HttpServletRequest httpRequest) {
        String phone = PhoneNumbers.normalize(request != null ? request.getPhone() : null);
        if (!PhoneNumbers.isValid(phone)) {
            throw new MateClawException("err.auth.invalid_phone", 400, "手机号格式不正确");
        }
        if (authService.isPhoneRegistered(phone)) {
            throw new MateClawException("err.auth.username_exists", 409, "该手机号已注册，请直接登录");
        }
        verificationCodeService.sendRegisterCode(phone, ClientIp.from(httpRequest));
        return R.ok();
    }
```

（`AuthController` 仍持有 `verificationCodeService` 字段，不要删除。）

- [ ] **Step 5: 更新 `AuthServiceRegisterTest`**

5a. 删除 import（原第 18 行）`import vip.mate.auth.sms.VerificationCodeService;` 和静态 import（原第 35 行）`import static org.mockito.Mockito.doThrow;`（删两个验证码用例后不再使用）。

5b. 删除 mock 字段（原第 56-57 行）：

```java
    @Mock
    private VerificationCodeService verificationCodeService;
```

5c. `validRequest()`（原第 281-287 行）删除 `request.setCode(...)` 行，改为：

```java
    private RegisterRequest validRequest() {
        RegisterRequest request = new RegisterRequest();
        request.setPhone("13800138000");
        request.setPassword("pass1234");
        return request;
    }
```

5d. **删除**两个用例方法：`registerRejectsInvalidVerificationCode`（原第 139-152 行）、`registerRejectsMissingVerificationCode`（原第 154-167 行）。

5e. `registerMapsDuplicateKeyRaceToUsernameExists`（原第 220-232 行）删除最后一行 `verify(verificationCodeService).releaseSendLock("13800138000");`，方法结尾改为：

```java
        assertEquals("err.auth.username_exists", ex.getMsgKey());
        verifyNoInteractions(workspaceService);
    }
```

5f. **新增**短密码用例（放在 `registerRejectsBlankPassword` 方法之后）：

```java
    @Test
    void registerRejectsShortPassword() {
        RegisterRequest request = validRequest();
        request.setPassword("123");

        MateClawException ex = assertThrows(MateClawException.class, () -> authService.register(request));

        assertEquals("err.auth.password_too_short", ex.getMsgKey());
        verify(userMapper, never()).insert(any(UserEntity.class));
        verifyNoInteractions(workspaceService);
    }
```

- [ ] **Step 6: 运行后端测试**

Run（JDK 21）：

```bash
cd /Users/linfeng/work/dev_work/mateclaw/.claude/worktrees/clever-hoover-52c9f8
JAVA_HOME=$(/usr/libexec/java_home -v 21) mvn -q -pl mateclaw-server test \
  -Dtest='AuthServiceRegisterTest,AuthControllerRegisterTest,AuthControllerSendCodeTest,VerificationCodeServiceTest,InMemoryVerificationCodeStoreTest' \
  -am -Dsurefire.failIfNoSpecifiedTests=false
```

Expected: BUILD SUCCESS，全部用例通过（验证码模块测试不受影响）。

- [ ] **Step 7: 提交**

```bash
git add mateclaw-server/src/main/java/vip/mate/auth/model/RegisterRequest.java \
        mateclaw-server/src/main/java/vip/mate/auth/service/AuthService.java \
        mateclaw-server/src/main/java/vip/mate/auth/controller/AuthController.java \
        mateclaw-server/src/test/java/vip/mate/auth/service/AuthServiceRegisterTest.java
git commit -m "feat(注册后端): 去短信验证码, 注册仅手机号+密码, 密码≥6位"
```

---

## Task 2：前端 — api 与 i18n

**Files:**
- Modify: `mateclaw-ui/src/api/index.ts:127-130`
- Modify: `mateclaw-ui/src/i18n/locales/zh-CN.ts`（login 段，约 2792-2816）
- Modify: `mateclaw-ui/src/i18n/locales/en-US.ts`（login 段，约 2780-2804）

- [ ] **Step 1: `api/index.ts` 调整 authApi**

将原第 127-130 行：

```ts
  register: (data: { phone: string; code: string; password: string; nickname?: string }) =>
    http.post<LoginResponse>('/auth/register', data),
  sendRegisterCode: (data: { phone: string }) =>
    http.post<void>('/auth/send-register-code', data),
```

替换为（去掉 `code`，删除 `sendRegisterCode`）：

```ts
  register: (data: { phone: string; password: string; nickname?: string }) =>
    http.post<LoginResponse>('/auth/register', data),
```

- [ ] **Step 2: `zh-CN.ts` 更新 login 段**

2a. `fields` 对象：删除 `code: '验证码',`，新增 `confirmPassword: '确认密码',`。结果：

```ts
    fields: {
      username: '用户名',
      password: '密码',
      phone: '手机号',
      confirmPassword: '确认密码',
      nickname: '昵称',
    },
```

2b. `placeholders` 对象：删除 `code: '请输入验证码',`，新增 `confirmPassword: '请再次输入密码',`。结果：

```ts
    placeholders: {
      username: '请输入用户名',
      password: '请输入密码',
      phone: '请输入手机号',
      confirmPassword: '请再次输入密码',
      nickname: '昵称（可选）',
    },
```

2c. 将原第 2811-2815 行（`registerFailed` 到 `sendCodeFailed`）：

```ts
    registerFailed: '注册失败，请检查手机号和验证码',
    getCode: '获取验证码',
    resendIn: '{n}s 后重试',
    phoneRequired: '请先输入手机号',
    sendCodeFailed: '验证码发送失败，请稍后重试',
```

替换为：

```ts
    registerFailed: '注册失败，请检查手机号和密码',
    passwordTooShort: '密码长度至少6位',
    passwordMismatch: '两次输入的密码不一致',
```

- [ ] **Step 3: `en-US.ts` 更新 login 段**

3a. `fields`：删除 `code: 'Verification code',`，新增 `confirmPassword: 'Confirm password',`。结果：

```ts
    fields: {
      username: 'Username',
      password: 'Password',
      phone: 'Phone',
      confirmPassword: 'Confirm password',
      nickname: 'Nickname',
    },
```

3b. `placeholders`：删除 `code: 'Verification code',`，新增 `confirmPassword: 'Re-enter password',`。结果：

```ts
    placeholders: {
      username: 'Enter username',
      password: 'Enter password',
      phone: 'Enter phone number',
      confirmPassword: 'Re-enter password',
      nickname: 'Nickname (optional)',
    },
```

3c. 将原第 2799-2803 行（`registerFailed` 到 `sendCodeFailed`）：

```ts
    registerFailed: 'Registration failed. Please check your phone and code.',
    getCode: 'Get code',
    resendIn: 'Retry in {n}s',
    phoneRequired: 'Enter your phone number first',
    sendCodeFailed: 'Failed to send code, please try again later',
```

替换为：

```ts
    registerFailed: 'Registration failed. Please check your phone and password.',
    passwordTooShort: 'Password must be at least 6 characters',
    passwordMismatch: 'The two passwords do not match',
```

- [ ] **Step 4: 类型检查（确认 i18n/api 改动不破坏编译）**

Run：

```bash
cd /Users/linfeng/work/dev_work/mateclaw/.claude/worktrees/clever-hoover-52c9f8/mateclaw-ui
pnpm build
```

Expected: 此时 `Login.vue` 仍引用 `code`/`sendRegisterCode`/旧 i18n 键 → **预计 vue-tsc 报错**（Login.vue 未改）。这是预期的，Task 3 修完再整体验证；本步只确认 api/i18n 文件本身语法正确（无该两文件的报错）。

- [ ] **Step 5: 提交**

```bash
cd /Users/linfeng/work/dev_work/mateclaw/.claude/worktrees/clever-hoover-52c9f8
git add mateclaw-ui/src/api/index.ts mateclaw-ui/src/i18n/locales/zh-CN.ts mateclaw-ui/src/i18n/locales/en-US.ts
git commit -m "feat(注册前端): api/i18n 去验证码, 增确认密码与密码校验文案"
```

---

## Task 3：前端 — Login.vue 注册表单

**Files:**
- Modify: `mateclaw-ui/src/views/Login.vue`

字段目标顺序（注册）：手机号 → 密码 → 确认密码 → 昵称。复用现有带眼睛切换的共享密码框（`activePassword`，登录绑 `form.password`、注册绑 `registerForm.password`）；确认密码框跟随同一 `showPassword`、自身不带眼睛按钮。

- [ ] **Step 1: 模板 — 注册身份字段改为仅手机号**

将原第 45-91 行的 `<template v-else> ... </template>`（含 phone、code-wrap、nickname 三块）整体替换为单个手机号块：

```html
        <div v-else class="input-wrap">
          <input
            v-model="registerForm.phone"
            type="tel"
            class="form-input"
            :placeholder="t('login.placeholders.phone')"
            :aria-label="t('login.fields.phone')"
            autocomplete="tel"
            required
          />
        </div>
```

（原第 33-43 行的登录用户名 `<div v-if="mode === 'login'">` 保持不变；共享密码框原第 93-113 行保持不变。）

- [ ] **Step 2: 模板 — 在共享密码框之后插入确认密码 + 昵称（仅注册）**

在共享密码框结束 `</div>`（原第 113 行）之后、`<div v-if="errorMsg" ...>`（原第 115 行）之前，插入：

```html
        <template v-if="mode === 'register'">
          <div class="input-wrap">
            <input
              v-model="registerForm.confirmPassword"
              :type="showPassword ? 'text' : 'password'"
              class="form-input"
              :placeholder="t('login.placeholders.confirmPassword')"
              :aria-label="t('login.fields.confirmPassword')"
              autocomplete="new-password"
              required
            />
          </div>
          <div class="input-wrap">
            <input
              v-model="registerForm.nickname"
              type="text"
              class="form-input"
              :placeholder="t('login.placeholders.nickname')"
              :aria-label="t('login.fields.nickname')"
              autocomplete="nickname"
            />
          </div>
        </template>
```

- [ ] **Step 3: 脚本 — 清理 import 与 registerForm**

3a. 将原第 131 行：

```ts
import { computed, onBeforeUnmount, reactive, ref } from 'vue'
```

改为（去掉 `onBeforeUnmount`）：

```ts
import { computed, reactive, ref } from 'vue'
```

3b. 将原第 148 行 `registerForm`：

```ts
const registerForm = reactive({ phone: '', code: '', password: '', nickname: '' })
```

改为：

```ts
const registerForm = reactive({ phone: '', password: '', confirmPassword: '', nickname: '' })
```

- [ ] **Step 4: 脚本 — 删除验证码相关逻辑**

删除原第 150-192 行整段（含 `sendingCode`、`codeCountdown`、`codeTimer` 声明，`onCodeInput`、`startCodeCountdown`、`handleSendCode` 函数，以及 `onBeforeUnmount(...)` 清理块）。删除后，`const activePassword = computed({...})`（原第 194 行起）紧接 `registerForm` 声明之后。

- [ ] **Step 5: 脚本 — 重写 `handleRegister`**

将原第 255-274 行 `handleRegister` 整体替换为：

```ts
async function handleRegister() {
  if (!registerForm.phone || !registerForm.password || !registerForm.confirmPassword) return
  if (registerForm.password.length < 6) {
    errorMsg.value = t('login.passwordTooShort')
    return
  }
  if (registerForm.password !== registerForm.confirmPassword) {
    errorMsg.value = t('login.passwordMismatch')
    return
  }
  loading.value = true
  errorMsg.value = ''
  try {
    const payload = {
      phone: registerForm.phone,
      password: registerForm.password,
      nickname: registerForm.nickname || undefined,
    }
    const res = await authApi.register(payload)
    const data = (res.data || res) as LoginResponse
    await finishAuth(data, registerForm.phone)
  } catch (e: any) {
    errorMsg.value = e?.message || t('login.registerFailed')
  } finally {
    loading.value = false
  }
}
```

- [ ] **Step 6: 样式 — 删除失效的验证码 CSS**

删除 `.code-wrap`（原第 376-382 行）与 `.code-btn` 及其状态（原第 384-411 行，含 `.code-btn`、`:disabled`、`:hover:not(:disabled)`、`:focus-visible`）。`.input-wrap`、`.form-input` 等保持不变。

- [ ] **Step 7: 构建 + 单测验证**

Run：

```bash
cd /Users/linfeng/work/dev_work/mateclaw/.claude/worktrees/clever-hoover-52c9f8/mateclaw-ui
pnpm build && pnpm test
```

Expected: `pnpm build` 通过（vue-tsc 无 `code`/`sendRegisterCode`/旧 i18n 键残留报错，vite build 成功）；`pnpm test`（vitest run）全绿（`onboardingBrand.test.ts` 不受影响）。

- [ ] **Step 8: 提交**

```bash
cd /Users/linfeng/work/dev_work/mateclaw/.claude/worktrees/clever-hoover-52c9f8
git add mateclaw-ui/src/views/Login.vue
git commit -m "feat(注册前端): Login.vue 去验证码UI, 加确认密码与前端校验"
```

---

## 最终验证（全部任务完成后）

- [ ] **后端**：`JAVA_HOME=$(/usr/libexec/java_home -v 21) mvn -q -pl mateclaw-server test -Dtest='AuthServiceRegisterTest' -am -Dsurefire.failIfNoSpecifiedTests=false` → 通过。
- [ ] **前端**：`pnpm -C mateclaw-ui build && pnpm -C mateclaw-ui test` → 通过。
- [ ] **手动冒烟**（可选，`pnpm -C mateclaw-ui dev`）：注册页仅 手机号/密码/确认密码/昵称；验证三条路径——① 两次密码不一致 → 提示"两次输入的密码不一致"不提交；② 密码 < 6 位 → 提示"密码长度至少6位"不提交；③ 正常手机号 + 一致的 6 位以上密码 → 注册成功并进入首页。

---

## Self-Review 记录

- **Spec 覆盖**：去验证码（T1 S2-3 / T3 S1,4 / T2 S1）、密码两次确认仅前端（T3 S5）、密码≥6 前后端（T1 S3 + T3 S5）、短信模块保留标弃用（T1 S4）、i18n 增删（T2 S2-3）、测试更新（T1 S5）——均有对应任务。
- **占位符**：无 TBD/TODO，所有代码步骤含完整代码。
- **类型一致**：后端新错误键 `err.auth.password_too_short` 在 AuthService（T1 S3）与测试（T1 S5f）一致；前端 i18n 键 `passwordTooShort`/`passwordMismatch`/`confirmPassword`（T2）与 Login.vue 引用（T3）一致；`register` 入参去 `code` 在 api（T2 S1）与调用（T3 S5）一致。
